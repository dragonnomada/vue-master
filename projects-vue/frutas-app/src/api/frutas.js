export async function getPriceByClientId(clientId) {
    // TODO: Consumir el servidor
    
    // const response = await fetch(url, options)

    // if (response.status !== 200)

    // const output = await response.json()

    // const { newPrice } = output

    return 49.88
}

export async function getPriceByCode(code) {
    // TODO: Consumir el servidor
    
    // const response = await fetch(url, options)

    // if (response.status !== 200)

    // const output = await response.json()

    // const { newPrice } = output

    return 49.88
}

export async function updateFrutaName(name) {
    // TODO: Consumir el servidor
    
    // const response = await fetch(url, options)

    // if (response.status !== 200)

    // const output = await response.json()

    // const { newPrice } = output

    return 49.88
}